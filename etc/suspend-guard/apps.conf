# suspend-guard app list
# One process name per line, exactly as it appears in `pgrep -l <name>` or
# `ps -C <name>` — not the .desktop file name or window title.
# Lines starting with # are ignored.

code
zen-bin

# Uncomment / add your actual browser process name — check with:
#   pgrep -l -f "firefox|brave|chrom|opera"
# firefox
# firefox-bin
# brave
# brave-browser
# chromium
# chromium-browser
# google-chrome-stable
# opera
